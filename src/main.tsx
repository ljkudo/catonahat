import React from "react";
import ReactDOM from "react-dom/client";
import CatonahatHome from "./CatonahatHome";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <CatonahatHome />
  </React.StrictMode>
);
